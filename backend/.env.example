# ============================================
# Backend Environment Variables
# ============================================
# Copy this file to .env and update values for your environment
# Command: Copy-Item .env.example .env (Windows) or cp .env.example .env (Mac/Linux)

# ============================================
# LOCAL DEVELOPMENT VALUES
# ============================================
PORT=3001
NODE_ENV=development
DB_PATH=./data/revenue.db
FRONTEND_URL=http://localhost:3000

# ============================================
# PRODUCTION VALUES (for Render)
# ============================================
# PORT - Render will auto-assign (use $PORT in code, already configured)
# NODE_ENV=production
# DB_PATH=./data/revenue.db
# FRONTEND_URL=https://your-frontend-app.vercel.app

# ============================================
# Variable Descriptions
# ============================================
# PORT: Server port (3001 for local, auto-assigned in production)
# NODE_ENV: Environment mode (development or production)
# DB_PATH: Path to SQLite database file
# FRONTEND_URL: Frontend URL for CORS configuration
