services:
  - type: web
    name: revenue-intelligence-backend
    env: node
    rootDir: backend
    buildCommand: npm install --production=false && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_PATH
        value: ./data/revenue.db
    healthCheckPath: /health
